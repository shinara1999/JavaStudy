package com.sist.manager2;
import com.sist.manager.*; // 다른패키지이므로 import 사용
public class Sub extends Super{
	// 변수 / 메소드
	/*
	 *  private int a; // 자신의 클래스 안에서만 접근 가능
	 *	protected int b; // 같은 패키지 안에서만 접근 가능 but 상속을 내리면 패키지와 관계없이 접근 가능
	 *	int c; // 같은 패키지 안에서만 접근 가능
	 *	public int d; // 모든 클래스에 접근 가능
	 */
	public Sub() {
		
	}
}
